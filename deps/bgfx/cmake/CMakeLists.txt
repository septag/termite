# CMakeLists autogenerated by GENie
cmake_minimum_required(VERSION 2.8.4)

add_subdirectory(bgfx)
add_subdirectory(bimg)
add_subdirectory(bimg_decode)
add_subdirectory(bimg_encode)

set_target_properties(bgfx PROPERTIES FOLDER Deps)
set_target_properties(bimg PROPERTIES FOLDER Deps)
set_target_properties(bimg_decode PROPERTIES FOLDER Deps)
set_target_properties(bimg_encode PROPERTIES FOLDER Deps)

if (BUILD_TOOLS)
    add_subdirectory(example-glue)
    add_subdirectory(example-common)
    add_subdirectory(spirv-opt)
    add_subdirectory(glslang)
    add_subdirectory(glsl-optimizer)
    add_subdirectory(fcpp)
    add_subdirectory(shaderc)

    set_target_properties(example-glue PROPERTIES FOLDER Deps/bgfx_deps)
    set_target_properties(example-common PROPERTIES FOLDER Deps/bgfx_deps)
    set_target_properties(shaderc PROPERTIES FOLDER Deps)
    set_target_properties(spirv-opt PROPERTIES FOLDER Deps/shaderc_deps)
    set_target_properties(glslang PROPERTIES FOLDER Deps/shaderc_deps)
    set_target_properties(glsl-optimizer PROPERTIES FOLDER Deps/shaderc_deps)
    set_target_properties(fcpp PROPERTIES FOLDER Deps/shaderc_deps)
endif()
