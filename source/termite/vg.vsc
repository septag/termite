$input a_position, a_texcoord0, a_color0
$output v_texcoord0, v_color0

#include <bgfx_shader.sh>

void main()
{
    vec4 pos = mul(u_model[0], vec4(a_position, 0.0f, 1.0f));
    gl_Position = mul(u_proj, pos);
    v_texcoord0 = a_texcoord0;
    v_color0 = a_color0;
}